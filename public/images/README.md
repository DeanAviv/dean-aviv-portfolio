Placeholder images live here so content can be edited without touching components.

Replace any SVG with a real PNG, JPG, WebP, or SVG and update the matching `image`
path in `src/data/projects.ts` or `src/data/studentProjects.ts` if the filename changes.

Suggested sizes:
- Project cards: 1600 x 1000
- Student project cards: 1200 x 900
